# Environment Variables for Lead City MFB Website
# Copy this file to .env for local development

# Application Environment
NODE_ENV=development
PORT=5000

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/leadcity_mfb
PGHOST=localhost
PGPORT=5432
PGDATABASE=leadcity_mfb
PGUSER=leadcity_user
PGPASSWORD=your_password_here

# Note: On Render, these variables are automatically set from the database service
# You only need to set DATABASE_URL manually if using an external database